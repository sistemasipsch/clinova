/**
 * Configuración central de la URL base del backend en producción.
 * Dominio permanente e infinito: https://api.clinicalhouse.co
 */
export const API_BASE_URL = import.meta.env.VITE_API_URL || 'https://api.clinicalhouse.co';
